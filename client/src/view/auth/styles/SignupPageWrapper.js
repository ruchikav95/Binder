import styled from 'styled-components';
import Wrapper from 'view/auth/styles/Wrapper';

const SignupPageWrapper = styled(Wrapper)`
  background-image: url(/images/signupPage.JPG);
`;

export default SignupPageWrapper;
